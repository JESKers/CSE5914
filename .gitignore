# Python
__pycache__/
*.pyc
.venv/
venv/

# Environment
.env

# Local vector DB / generated files
milvus_demo.db
*.db
*.sqlite
*.sqlite3

# OS/editor
.DS_Store
.vscode/
.idea/

# Docker
/volumes
*.py[cod]
.venv/
venv/
.pytest_cache/
evaluation/results/

# Env / secrets
.env

# Data — commit the catalog inputs/outputs and synthetic reference tables, but
# keep runtime artifacts and any unlisted local data out of Git.
/data/*
!/data/.gitkeep
!/data/README.md
!/data/data.csv
!/data/cars_clean.json
# ...except the hand-written synthetic reference tables the assistant needs
# (small JSON, not reproducible from Kaggle data).
!/data/synth

# RAG vector store (rebuild with rag/build_index.py)
rag/faiss_index/

# OS / editor
.DS_Store
